# Documentation

## Must Create

- [ ] `README.md` — Project overview, architecture diagram, quickstart guide (both NPM package integration and Go agent setup)
- [ ] `kagami.example.toml` — Annotated example config file with comments explaining each option
- [ ] `packages/kagami/README.md` — NPM package docs: installation, `createKagami()` API, required Worker bindings (`TUNNEL`, `KAGAMI_DB`, `KAGAMI_PROJECT_SECRET`, `KAGAMI_BASE_DOMAIN`), wrangler.toml setup, D1 migration
- [ ] `examples/basic-worker/README.md` — Example Worker setup: wrangler config, D1 database creation, secret setup, deployment

## Must Update

(No existing docs to update — this is the initial project setup.)
